# v7 旁白独立审查

- 审查日期：2026-08-23
- 审查对象：`topics/constructible-17-gon/audio/constructible_17_gon_v7_narration.txt`
- 结论：`Ready for TTS`

## 阻塞问题

- 无。旁白已经补齐“交点来源—方程次数”“箭头投影—`c_k`”“和角差角—积化和差”“实数根—有理根—有理系数分解”“候选值的本题内推导”五条因果桥。有理根段落不再让“无有理根”承担“无零点”的含义，也不再要求观众直接接受有理根定理。

## 关键时间线

| 节拍 | 观众此时已知 | 本拍新信息 | 之后才允许使用 |
| --- | --- | --- | --- |
| 尺规能力边界 | 加减乘除和开平方可作 | 新点只来自三类交点 | 交点方程至多二次 |
| 七边形投影 | 单位箭头及横轴 | 一对箭头的横向分量之和定义为 `c_k` | `1+c1+c2+c3=0` |
| 积化和差 | 需要把 `c` 的乘积变回 `c` 的和 | 和角、差角相加后正弦项抵消 | `c_i c_j=c_{i+j}+c_{i-j}` |
| 三次式的三个零点 | 已得到七边形三次式 | 图像有三个实数根；目标 `u` 是右侧正根 | “有实数根”与“有有理根”的区别 |
| 有理系数分解 | 已看到三个实数根 | 三次式若能在有理数系数范围内分解，必有有理的一次因式 | 用有理根判断是否可约 |
| 候选值来源 | 已知要检查有理根 | 把最简分数 `p/q` 代入本题并乘 `q³`，两种整理推出 `q=1`、`p=±1` | 代入候选与不可约三次结论 |
| 十七边形第一层 | A、B 各包含哪些 `c` | 圆周箭头按 `1/A/B` 着色 | `1+A+B=0` 与 `A+B=-1` |

## 公式口播与多音字

- 三角函数在旁白中使用 `sine`、`cosine`，画面仍使用标准 `\sin`、`\cos`。
- `t²-At-1=0` 等式改读为“t 的平方减 A 乘 t 再减一等于零”，避免字母粘连。
- 基本角在旁白中使用中文描述，不直接朗读 `θ`、`iθ`、`jθ`。
- 避免用“这一行”，统一说“这个式子”“这两个式子”或直接描述运算。
- `q³`、`p³` 在口播中使用“q 的三次方”“p 的三次方”，不让 TTS 直接朗读上标。

## 生成后检查

- 试听 `c1` 到 `c8`、A 到 F、P、p、q 的停顿。
- 检查 `sine`、`cosine` 及高斯—旺策尔的实际发音。
- 按段落边界重新计算二十四个场景的目标时长，不能复用 v6 的 `finish(...)` 数值。
