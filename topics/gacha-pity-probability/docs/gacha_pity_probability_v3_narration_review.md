# 原神抽卡概率 v3 旁白审校

- 审校对象：`audio/gacha_pity_probability_v3_narration.txt`
- 最终结论：Ready for final TTS；无内容性阻断行。现有音频只用于建立静音预览时间轴；正式配音时仍需为 `q(n)`、`PMF`、`CDF` 设置逐字母或自定义读法，并重新校时生成最终SRT
- 最终复审：2026-08-30；已按当前 v3 旁白逐行复核口语表达、信息顺序、数学数值、TTS 风险和单行字幕长度

## 本轮主要调整

- 去掉“数字卡”“数字打架”“这颗五星是谁”“把两层揉成一个概率”等制作腔或生硬比喻，全文改为直接描述观众正在看的抽卡过程。
- 用具体人数解释第 50 抽：约 `7446` 人仍在等待，本抽首次出金期望值约 `44.68` 人，再分别除以不同分母得到 `0.6%`、`0.447%` 和 `25.99%`，不再用取整后的45人计算0.447%。
- 固定概率对照要求每抽同时更新“本抽出金人数”和“仍在等待人数”，让动画持续呈现人群如何分流。
- 三条曲线改为逐条引入，首次给出正式名称；`PMF` 与 `CDF` 随后统一使用容易记的简称：
  - 条件概率：保留标准名称，画面标作 `q(n)`
  - 概率质量函数 `PMF`：**首金分布**
  - 累积分布函数 `CDF`：**累计出金率**
- 把三条曲线的关系改写为“每抽有多少人出金、他们在哪一抽首次出金、目前共有多少人出过金”，不再使用“柱子逐项累加”等不自然说法。
- 捕获明光只在专门段落首次出现；前面的双峰段直接声明只使用 `50/50 + 歪后必UP` 的简化规则，不提前否定尚未介绍的机制。
- 每句尽量控制为一个信息点，便于 480p 审阅版使用较小字号和单行字幕。

## 数学与口径检查

- 固定 `0.6%` 模型保留了关键人数与数值：`7446`、`45`、`5818`、`0.447%`、`0.351%`、`25.99%`、`41.82%`。
- 仅硬保底模型保留 `5853`、`58.53%`、`41.47% → 100%`，并在展示结果前明确它不是官方逐抽概率。
- 综合概率段保留 `69.7抽 → 1.435%` 与 `62.5抽 → 1.6%` 的比较，只推出“90抽前某些位置的条件概率高于0.6%”，不声称具体软保底起点或增幅。
- 三条候选曲线只称为“符合公开数字的示例”，没有暗示任何一条是官方答案。
- UP 双峰仍按 `50/50` 混合解释；两段独立等待时间相加后，总等待时间的分布由离散卷积得到。示例沿用前文满足公开约束的候选曲线，不再另造一条无来源的形状函数。
- 捕获明光的 `0.018%` 与 `55%` 分别说明分母；连续三轮也明确为三次完整的“歪到UP”过程。

## 观众已知信息检查

- 在正式名称出现前，观众已经通过第 50 抽人数动画见过三种分母。
- `PMF` 与 `CDF` 的简称均在各自曲线单独出现时引入；条件概率沿用标准名称，不要求观众同时记住三个新简称。
- 三条曲线只在逐条讲完后缩小并列，用于回顾关系。
- 捕获明光首次出现时同时给出用途；前文没有出现“先关掉捕获明光”或“暂不计捕获明光”。
- 官方规则、教学模型与社区拟合三种信息来源保持分开。

## TTS 与字幕检查

- 当前旁白在首次定义和结尾回顾处保留 `q(n)`、`PMF`、`CDF`，字幕照常显示这些标准写法；TTS 输入层需把它们映射为“q，下标 n”“P M F”“C D F”，不要依赖引擎自动猜读。
- `0.994` 的幂、乘积号和卷积公式不写入口播，避免逐字符朗读。
- 数值在字幕中应继续显示为 `0.6%`、`1.6%`、`62.5抽`，不要改写成中文数字。
- `PMF` 与 `CDF` 需在 TTS 中设置为逐字母读法，并在首次出现处保留短停顿。
- 旁白文本已通过信息顺序与日常表达审阅；后续需检查新版画面中的人数、曲线名称和字幕是否同帧出现。

## 最终复审结果

- 对话感：句子直接描述抽卡人数、分母和曲线变化，没有恢复“数字打架”“把两层揉成一个概率”等生硬表达。
- 信息顺序：先用第 50 抽人数建立三种分母，再逐条命名曲线；三条曲线讲完后才缩小并列。捕获明光首次出现于专门介绍段，双峰段没有提前提及。
- 数学准确性：复核了 `7446`、`44.68`、`2599`、`5818`、`5853`、`0.447%`、`0.351%`、`25.99%`、`41.82%`、`58.53%`、`69.7抽`、`1.435%` 和 `62.5抽`，数值及分母均一致。
- 字幕长度：共 `117` 个非空句，最长 `29` 个字符，没有超过 `30` 字的句子；配合缩小后的项目规范字体，可按单句单 cue 制作大多数单行字幕。
- 阻断项：无。`q(n)`、`PMF`、`CDF` 的自动读法属于 TTS 配置风险，不需要为此改动观众看到的旁白文本。
